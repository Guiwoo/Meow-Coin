const meow = "Meow coin",
    age = 29,
    gender = "male";

const sayHi = (meow,age,gender)=>{
    console.log(`Hello ${meow}, you are ${age} years old, your gender is ${gender}`)
}

sayHi(meow,age,gender);

export {};